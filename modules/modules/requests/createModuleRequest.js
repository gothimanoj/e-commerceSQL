// const validation = require("../../../middlewares/validation");

// const rules = {
//     moduleName: "required|string|min:2",
//     createModule: "boolean",
// };

// module.exports = (req, res, next) => validation(req, res, next, rules, {});
